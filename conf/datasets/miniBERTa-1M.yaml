# wikitext103.yaml
#   Configuration for miniBERTa dataset (https://huggingface.co/datasets/ehoseini/miniBERTa).
---
dataset:
    id: /om/user/ehoseini/miniBERTa
    name: miniBERTa-1M
    validation_ratio: null

    # Number of Preprocessing Workers
    num_proc: 4

    # Number of Evaluation Preprocessing Workers
    eval_num_proc: 4
